# Coding Guidelines

- Keine GUI in Runner, Logon/Logoff oder Offline-Tasks.
- Keine hardcodierten Customer-Pfade oder UNC-Pfade.
- Logging ist Pflicht (Write-Log im SDK verwenden).
- ExitCodes definieren und dokumentieren.
- PSScriptAnalyzer: keine Errors.
