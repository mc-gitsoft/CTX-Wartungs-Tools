param(
    [ValidateSet("User","Admin")]
    [string]$UiMode = "User"
)

Write-Output "Host GUI ist noch nicht implementiert. Starte ein Tool direkt (Start.ps1)."
Write-Output "Gewuenschter Modus: $UiMode"
